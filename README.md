# sysqube_intern

## Flutter Project for SysQube Intership Task

### Installation Guide

1. Download the zip file or clone the project
2. In the lib/utils/constants, replace your actuali apikey
3. In the project root run:
  
    flutter pub get

    flutter run 

### Dependencies Used:

dependencies:
  cached_network_image: ^3.4.1
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_spinkit: ^5.2.2
  get: ^4.7.3
  http: ^1.6.0
  intl: ^0.20.2
  path: ^1.9.1
  path_provider: ^2.1.5
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
### Core Dependencies:

    1. GetX - State Management (Themes, favs)
            - Dependency Injection (Controllers)
    2. SQLite - Local Storage (Storing Favourite Table)
    3. http - to send/fetch API request

